# RealmCarthageExample
A sample project demonstrating the error with trying to build Realm as an XCFramework

Carthage fails every time, on multiple different Macs, to build Realm 5.5.2 as an XCFramework
